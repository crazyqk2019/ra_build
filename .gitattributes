# 自动识别并规范化文本文件
* text=auto

# Bash 脚本必须使用 LF，避免 Windows 检出后无法执行
*.sh text eol=lf

# Windows 批处理保持 CRLF
*.cmd text eol=crlf

# 文档及 Git 配置文件使用 LF
*.md text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
